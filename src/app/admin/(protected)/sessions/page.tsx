import Link from "next/link";
import { db } from "@/lib/db";
export default async function SessionsPage({searchParams}:{searchParams:Promise<{status?:"CREATED"|"IN_PROGRESS"|"COMPLETED"|"TIMEOUT"|"ABANDONED";participant?:string}>}){const p=await searchParams;const sessions=await db.trainingSession.findMany({where:{status:p.status,participant:p.participant?{name:{contains:p.participant,mode:"insensitive"}}:undefined},include:{participant:true},orderBy:{createdAt:"desc"},take:100});return <><div className="eyebrow">Latihan</div><h1 style={{fontSize:"2.7rem"}}>Training Sessions</h1><div style={{marginBottom:16}}><a className="button secondary" href="/api/admin/sessions/export">Export CSV</a></div><form className="card grid-2" style={{marginBottom:20}}><input className="input" name="participant" placeholder="Nama participant" defaultValue={p.participant}/><select className="select" name="status" defaultValue={p.status}><option value="">Semua status</option><option>IN_PROGRESS</option><option>COMPLETED</option><option>TIMEOUT</option><option>ABANDONED</option></select><button className="button">Filter</button></form><div style={{overflowX:"auto"}}><table><thead><tr><th>Participant</th><th>Started</th><th>Duration</th><th>Questions</th><th>Score</th><th>Status</th></tr></thead><tbody>{sessions.map(s=><tr key={s.id}><td><Link href={`/admin/sessions/${s.id}`}>{s.participant.name}</Link></td><td>{s.startedAt?.toLocaleString("id-ID")??"—"}</td><td>{s.durationMinutes} menit</td><td>{s.questionCount}</td><td>{s.score?`${Number(s.score).toFixed(0)}%`:"—"}</td><td><span className="badge">{s.status}</span></td></tr>)}</tbody></table></div></>}
